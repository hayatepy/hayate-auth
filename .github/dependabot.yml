# Keep SHA-pinned actions and every uv lockfile current in grouped weekly PRs.
version: 2
updates:
  - package-ecosystem: github-actions
    open-pull-requests-limit: 0
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
  - package-ecosystem: uv
    open-pull-requests-limit: 0
    directories:
      - /
      - /examples/todo
      - /examples/mcp-oauth
    versioning-strategy: lockfile-only
    schedule:
      interval: weekly
